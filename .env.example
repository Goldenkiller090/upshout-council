# Upshout Council runs on your Claude subscription via the local Claude Code CLI
# (the Claude Agent SDK) — no API key needed. Just be logged in:
#
#   claude          # then /login  (or run: claude setup-token)
#
# Make sure ANTHROPIC_API_KEY is NOT set, or the SDK will bill the API instead
# of using your subscription.

# Optional: override the Upshot API base URL
# UPSHOUT_API_BASE=https://api-mainnet.upshotcards.net/api/v1
